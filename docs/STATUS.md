# 📍 STATUS — ตอนนี้อยู่ตรงไหน ต้องทำอะไรต่อ

> **session ใหม่: อ่านไฟล์นี้ก่อนไฟล์อื่นทั้งหมด** แล้วค่อยไป `CLAUDE.md` (กฎ + bug log + decision log)
> และ `docs/ERP-STOCK.md` (รายละเอียดสต๊อก) เฉพาะตอนต้องการ
>
> **อัปเดตล่าสุด: 2026-09-25** (ตรวจฐานข้อมูลซ้ำรอบบ่าย — ตัวเลขไม่เปลี่ยน) · ใครแก้ไฟล์นี้ ให้แก้วันที่ด้วย

---

## 1. สรุปใน 30 วินาที

ระบบ**สร้างเสร็จหมดแล้ว** ทั้ง PO Tracker (มือถือ+PC), ERP, คลังสต๊อก
**แต่ยังไม่มีใครใช้จริงสักโมดูล** — คอขวดตอนนี้**ไม่ใช่เรื่อง code** แต่เป็น 2 เรื่อง:

1. **พนักงาน 5 คนเข้าระบบไม่ได้มา 16 วันแล้ว** (ยังถือรหัส 4 หลัก ตั้งแต่เปลี่ยนเป็น 6 หลัก 2026-09-09)
2. **ตัวเลขในคลังสต๊อกยังเป็นชุดทดลอง** ยังไม่เคยเดินนับของจริง

> ⚠️ **อย่าเพิ่งเขียนฟีเจอร์ใหม่** จนกว่า 2 ข้อนี้จะจบ — ของที่สร้างไว้แล้วยังไม่ถูกใช้เลยแม้แต่อันเดียว
> การเพิ่มของใหม่ตอนนี้คือการเพิ่มของที่ไม่มีใครใช้
>
> **เฉพาะสต๊อก: ยังไม่ควรเพิ่มฟีเจอร์จนกว่าจะใช้จริงไปสัก 2–3 สัปดาห์** — ตอนนี้ยังไม่รู้ว่าคนใช้จริงจะติดตรงไหน รอดูของจริงก่อนแล้วค่อยแก้ตรงจุด

---

## 2. ตัวเลขจริงจากฐานข้อมูล (2026-09-25)

| เรื่อง | สถานะ |
|---|---|
| ผู้ใช้ | 10 คน · **6 หลักแล้ว 5 คน** (toun, supervisor, naree, manager, bk) · **ยังค้าง 4 หลัก 5 คน** (delivery, production, qc1, qc2, qc3) |
| สิทธิ์สต๊อก | ✅ เปิดให้ทุก role แล้ว — staff มี `stockIn`/`stockOut` · ไม่ใช่คอขวดอีกต่อไป |
| วัสดุในคลัง | 16 รายการใช้งาน = ของจริง 15 + **ของทดลองเหลือ "สีขาว" 1 ตัว** (toun ปิดของทดลองไปแล้ว 5) |
| ประวัติสต๊อก | 19 แถว — **ยังเป็นชุดทดลอง** ยังไม่เคยมี `stock_baseline_reset` หรือ `stock_stocktake` |
| การใช้งานจริง | รับเข้า/เบิกออกล่าสุด **2026-08-25** (ยังเป็นการทดลอง) |
| `qty_target` | ว่าง 13/16 → ป้าย "ควรสั่ง N" แทบไม่ทำงาน |
| ต่ำกว่าขั้นต่ำ | 3 รายการ — ทินเนอร์ 80/250 กก. · กระดาษทรายขัดน้ำ 140/290 แผ่น · สีโป๊วแดง 15/18 กก. **(เป็นตัวเลขทดลอง อาจไม่ตรงของจริง)** |
| Supabase | หลับไปแล้ว **3 ครั้ง** (ล่าสุด 2026-09-25) — **เช็ค `status` ก่อนสรุปว่าอะไรพังเสมอ** |
| Cache | `po-tracker-v51` |

---

## 3. 🔥 ต้องทำก่อน — เรียงลำดับพร้อมผลกระทบ

**ทั้ง 4 ข้อนี้เป็นงานของ toun ในโรงงาน ไม่ใช่งานเขียนโปรแกรม**

### ลำดับ 1 — ตั้งรหัส 6 หลักให้พนักงาน 5 คน ⏱️ 5 นาที
- **ทำที่**: ERP → การ์ด "ตั้งค่า · ผู้ใช้ · สิทธิ์" → แถวที่**ขึ้นกรอบแดง**
- **ทำไมต้องก่อนสุด**: **ข้อ 2-4 ทั้งหมดไม่มีความหมายถ้าคนเข้าระบบไม่ได้**
- **ถ้าไม่ทำ**: พนักงาน 5 คนใช้ระบบไม่ได้ถาวร · เขาพยายามเข้ามา 16 วันแล้วไม่ได้ อาจเลิกพยายามไปแล้ว
- **บล็อกอะไร**: ข้อ 3 (ไม่มีใครบันทึกสต๊อกได้) และการใช้ PO Tracker ทั้งหมด

### ลำดับ 2 — เดินนับของจริง แล้วกด "ตั้งต้นใหม่" ⏱️ ครึ่งวัน
- **ทำที่**: นับของ → คลังสต๊อก → แท็บ 🔐 ผู้ดูแล → 🧹 ตั้งต้นใหม่
- **ลำดับสำคัญ**: **นับก่อน แล้วค่อยกด** — หน้านั้นขอยอดที่นับได้ตั้งแต่ตอนกด ไม่ใช่ล้างให้ว่างแล้วค่อยกรอกทีหลัง
- **เตรียม**: จำนวนที่นับได้ + **ราคาต่อหน่วยล่าสุดจากใบส่งของ** (ราคานี้เป็นจุดเริ่มของต้นทุนเฉลี่ย ถ้าใส่มั่ว มูลค่าคลัง + รายงานกระทรวงเพี้ยนตามทั้งสาย)
- **อย่าลืม**: กด 🗑 ลบ "สีขาว" · **เซฟไฟล์ Excel สำรองที่เด้งมา**
- **ถ้าไม่ทำ**: ทุกตัวเลขที่ระบบรายงานไม่มีความหมาย · รายงานกระทรวงส่งไม่ได้
- **บล็อกอะไร**: ข้อ 3 และ 4

### ลำดับ 3 — ให้พนักงานเริ่มบันทึกรับเข้า/เบิกออกทุกวัน ⏱️ ต่อเนื่อง
- **ต้องทำหลัง**: ข้อ 1 (เข้าระบบได้) + ข้อ 2 (ตัวเลขตั้งต้นจริง)
- **effect ที่ได้**: ledger เริ่มมีข้อมูลจริง → รายงานรายเดือน/รายวันมีความหมาย → **Supabase เลิกหลับเอง** (มีคนเข้าทุกวัน)
- **ความเสี่ยงหลัก**: **ระบบสต๊อกไม่ล้มเพราะบันทึกช้า แต่ล้มเพราะคนไม่บันทึก** — ถ้า 2 อาทิตย์แล้วยังไม่มีใครบันทึก ให้กลับมาคิดเรื่องลดขั้นตอน (QR ติดชั้นวาง / ฟอร์มกระดาษพิมพ์แจก) ไม่ใช่เพิ่มฟีเจอร์

### ลำดับ 4 — ใส่ค่ากลาง (`qty_target`) ให้ครบ ⏱️ 15 นาที
- **ทำได้ตอนไหนก็ได้** แต่ได้ประโยชน์จริงหลังข้อ 2
- **effect**: ป้าย "ควรสั่ง N" เริ่มทำงาน → รู้ว่าต้องสั่งเพิ่มเท่าไหร่ ไม่ใช่แค่รู้ว่าใกล้หมด

---

## 4. งานฝั่งระบบที่ยังไม่ได้ทำ (รอ toun สั่ง)

**เรียงตามคุณค่าต่อธุรกิจ ไม่ใช่ตามความยากง่าย**

| # | งาน | ได้อะไร | ต้องมีอะไรก่อน | ประเมิน |
|---|---|---|---|---|
| 1 | **ระบบคิดต้นทุน PO** | รู้กำไรจริงต่อใบ ไม่ใช่เดา — ต่อยอดจากสต๊อกที่มีต้นทุนเฉลี่ยอยู่แล้ว | ข้อ 2-3 ข้างบน (ต้องมีข้อมูลสต๊อกจริงก่อน) | 3-5 วัน |
| 2 | **Dashboard สรุปยอดผลิต/ส่ง** | เห็นภาพรวมรายเดือน · ลูกค้ารายไหนเยอะสุด · ชิ้นไหนเยอะสุด | มี PO จริงสะสมพอสมควร | 2-3 วัน |
| 3 | **Line แจ้งเตือน** | เตือนลูกค้าเมื่อส่งของ / เตือนของใกล้หมด | ต้องตัดสินใจก่อนว่าเตือนใคร เรื่องอะไร | 2-3 วัน |
| 4 | **โหมดจุดงาน UI จริง** | แต่ละจุดเห็นเฉพาะที่ตัวเองต้องใช้ ลดความสับสน | ควรรอดูก่อนว่าคนใช้จริงติดตรงไหน | 2-3 วัน |
| 5 | **ระบบผสมสี** | บริบทครบแล้วใน `docs/BACKLOG-COLOR-MIXING.md` | **toun สั่งพักไว้** ทำสต๊อกให้จบก่อน | ยังไม่ประเมิน |
| 6 | **Backup อัตโนมัติ + error monitoring** | กันข้อมูลหายตอนใช้จริง | ควรทำก่อนข้อมูลเริ่มสะสมเยอะ | 1-2 วัน |
| 7 | **เชื่อม n8n** | automation ต่อยอด | ต้องรู้ก่อนว่าจะ automate อะไร | ยังไม่ชัด |
| 8 | **i18n พม่า** | ลูกน้องพม่าใช้ได้ | **มีคำถามค้าง 4 ข้อใน CLAUDE.md ที่ toun ยังไม่ตอบ** | 1-2 สัปดาห์ |

### ค้างจากเรื่องอื่น
- **Design ใหม่จาก Claude Design** — toun ส่งลิงก์ `ERP Home v2.dc.html` มาแต่ดึงเข้ามาไม่ได้ (`DesignSync` ต้องใช้เทอร์มินัลจริง) → ต้องให้ toun กด "Send to Claude Code Web" หรือก๊อปไฟล์มาวาง

---

## 5. ที่ทำไปแล้ว (สรุป ไม่ไล่วันที่)

| โมดูล | สถานะ | หมายเหตุ |
|---|---|---|
| **PO Tracker มือถือ** | ✅ ใช้งานจริงมาตั้งแต่ต้น | Realtime + offline + QC + Archive + Excel export |
| **PO Tracker PC** | ✅ ครบ 8 หน้าจอ | feature ตรงกับมือถือทุกอย่าง |
| **ERP หน้าแรก** | ✅ | KPI + การ์ดโมดูล + ตั้งค่าผู้ใช้ (admin เท่านั้น) |
| **คลังสต๊อก** | ✅ ครบ **แต่ยังไม่ถูกใช้** | ledger · ต้นทุนเฉลี่ย · รายวัน/รายเดือน · Excel เข้า-ออก · รายงานกระทรวง · บันทึกย้อนหลัง · ตั้งต้น/นับสต๊อก · **กันพลาด:** สองคนกดพร้อมกันยอดไม่หาย · เบิกเกินยอดไม่ได้ · ลบประวัติไม่ได้ (ยกเลิกได้อย่างเดียว) · ราคาเดือนที่ปิดแล้วไม่เปลี่ยนตามราคาใหม่ |
| **สิทธิ์ผู้ใช้** | ✅ แก้ผ่าน UI ได้ | 5 roles × 18 flags · admin ล็อกไว้กันตัวเอง lock out |
| **ล็อกอิน** | ✅ PIN 6 หลัก · ล็อกอินรอบเดียวใช้ได้ทุกหน้า · ตัดออกเมื่อไม่ใช้ 30 นาที | |
| **Bug ที่แก้แล้ว** | 46 ตัว | รายละเอียดใน `CLAUDE.md` |

---

## 6. กับดักที่ต้องรู้ก่อนแตะโค้ด

> รายละเอียดเต็มอยู่ใน `CLAUDE.md` — นี่คือ 6 ข้อที่พังบ่อยที่สุด

1. **Supabase หลับเอง** → เช็ค `status` ก่อนสรุปว่าอะไรพัง (หลับมาแล้ว 3 ครั้ง)
2. **แก้ไฟล์ที่ cache ไว้ = ต้อง bump `sw.js` CACHE_NAME** ไม่งั้นคนใช้ไม่ได้ของใหม่
3. **commit ที่ `Dev-PC` แล้ว merge ไป main/ERP/stock/design** — ห้าม commit ตรงบน main (มี hook กันไว้)
4. **เพิ่มพารามิเตอร์ DB function = DROP แล้ว CREATE** ห้าม `CREATE OR REPLACE` แล้วต้องเช็ค `pg_proc` เหลือแถวเดียว (BUG46 ทำ production ล่ม 20 นาที)
5. **ห้ามเขียน stock ตรง ๆ จาก client** ต้องผ่าน `apply_stock_movement()` เท่านั้น
6. **`adjust` เก็บยอดสัมบูรณ์ ไม่ใช่ส่วนต่าง** — อ่านย้อนหลังต้อง derive จาก `balance_after` chain เสมอ

---

## 7. วิธีอัปเดตไฟล์นี้

toun สั่งว่า **"บันทึก"** = ต้องได้ครบทั้ง 6 อย่าง:

1. ที่ผ่านมาทำอะไรไปแล้ว → §5
2. ต้องทำอะไรต่อ → §3 และ §4
3. เหลืออะไรบ้าง → §4
4. **ไล่ลำดับความสำคัญ** → §3 มีเลขลำดับชัด ๆ
5. **อะไร effect อะไร** → §3 มี "บล็อกอะไร" / "ต้องมีอะไรก่อน" ทุกข้อ
6. **session หน้าอ่านรู้เรื่อง** → §1 อ่าน 30 วิรู้เรื่อง · §6 กันพลาด

**เช็คตัวเลขจากฐานข้อมูลจริงก่อนเขียนเสมอ** อย่าลอกของเก่ามา — รอบที่แล้วเกือบเขียนผิดว่า "สิทธิ์สต๊อกยังไม่เปิด" ทั้งที่ toun เปิดไปแล้ว
