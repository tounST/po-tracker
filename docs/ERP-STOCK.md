# ERP + คลังสต๊อก — บันทึกสำหรับ session ถัดไป

> อัปเดตล่าสุด: 2026-08-05 · commit `4513a46` · เขียนเพื่อให้ session ใหม่เข้าใจบริบทได้ทันทีโดยไม่ต้องถาม toun ซ้ำ

---

## 1. ภาพรวม — ERP คืออะไรในโปรเจคนี้

ERP **ไม่ได้แทนที่** PO Tracker แต่เป็น **"เมนูใหญ่"** ที่ครอบอยู่ข้างบน และ PO Tracker เป็น 1 หมวดในนั้น

```
tounst.github.io/po-tracker/
├── index.html        ← router เดิม (คนงานเข้าหน้านี้ → PO Tracker)  ❗ห้ามแตะ
├── po-mobile.html    ← PO Tracker มือถือ (คนงาน)                    ❗ห้ามแตะโดยไม่จำเป็น
├── po-desktop.html   ← PO Tracker PC                                ❗ห้ามแตะโดยไม่จำเป็น
├── erp.html          ← 🆕 ERP Dashboard (ออฟฟิศ)
└── stock.html        ← 🆕 คลังสต๊อก
```

**ทางเข้า** (ต้องพิมพ์ใน address bar ของเบราว์เซอร์ ไม่ใช่ช่องค้นหาใน GitHub):

| ใคร | URL |
|---|---|
| ออฟฟิศ / toun | `https://tounst.github.io/po-tracker/erp.html` |
| คลังสต๊อก | `https://tounst.github.io/po-tracker/stock.html` |
| คนงาน | `https://tounst.github.io/po-tracker/po-mobile.html` (เดิม ไม่เปลี่ยน) |

**หลักการแยกส่วน:** ERP → กดเข้า PO ได้ แต่ PO → ไม่มีลิงก์กลับมา ERP
คนงานจึงไม่มีทางเจอ ERP · PWA ของคนงานยังผูกกับ `po-mobile.html` เหมือนเดิม
ทั้งสองใช้ **PIN ชุดเดียวกัน** จากตาราง `users`

---

## 2. Git branches

```
main   = production (GitHub Pages เสิร์ฟจากตรงนี้)
ERP    = ตัวใหญ่ umbrella
stock  = โมดูลคลังสต๊อก
```

ณ commit `4513a46` ทั้ง 3 branch + remote ทั้งหมดอยู่ที่ commit เดียวกัน
(branch `Dev` / `Dev-PC` ที่เขียนไว้ใน CLAUDE.md **ไม่มีอยู่ใน clone นี้แล้ว**)

**ตรวจก่อนจบงานทุกครั้ง:**
```bash
for b in main ERP stock origin/main origin/ERP origin/stock; do git rev-parse --short $b; done
# ต้องได้ค่าเดียวกันทั้งหมด
```

---

## 3. ฐานข้อมูล (Supabase `rkdxbxtakvisroxelrvq`)

### ตารางใหม่

| ตาราง | หน้าที่ |
|---|---|
| `stock_items` | วัสดุ — name, category, unit, qty_current, **qty_min** (ห้ามต่ำกว่า), **qty_target** (ค่ากลาง), cost_per_unit (เฉลี่ยเคลื่อนที่), source_country, ministry_seq, is_active |
| `stock_movements` | **ประวัติทุกการเคลื่อนไหว** — item_id, movement_type, qty, balance_after, **unit_cost**, ref_type/ref_id, **doc_no/supplier/photo_url**, **reverses_id**, created_by |

`movement_type` = `in` \| `out` \| `adjust` \| `initial` (มี CHECK constraint — **เพิ่มค่าใหม่ต้อง migrate ก่อน**)

⚠️ **`qty` ของ `adjust` เก็บ "ยอดใหม่แบบสัมบูรณ์" ไม่ใช่ส่วนต่าง**
เวลาอ่านย้อนหลังต้องคำนวณส่วนต่างจาก `balance_after` chain เท่านั้น (เคยพลาดมาแล้ว → BUG34)

### Functions (สำคัญมาก — ห้ามเขียน stock ตรง ๆ)

| Function | ทำอะไร |
|---|---|
| `apply_stock_movement(item, type, qty, unit_cost, po, note, by, doc_no, supplier, photo_url)` | ล็อกแถววัสดุ (`FOR UPDATE`) → เขียน ledger + คำนวณเฉลี่ยเคลื่อนที่ + อัปเดตยอด **ในทรานแซกชันเดียว** |
| `reverse_stock_movement(movement_id, note, by)` | ออกรายการกลับ ผูก `reverses_id` · กันยกเลิกซ้ำ/ยกเลิกรายการยกเลิก |

> ❗ **ห้ามกลับไป `insert` + `update` จากฝั่ง client เด็ดขาด** — นั่นคือสาเหตุ BUG32 (ยอดหายเมื่อสองคนกดพร้อมกัน)

### Storage
bucket `stock-docs` — public read, 5 MB, เฉพาะรูป
รูปถูกย่อฝั่ง client เป็นด้านยาว 1400px / JPEG q0.72 (~200 KB) ก่อนอัปโหลด

### สิทธิ์ใหม่ใน `role_permissions`
`stockIn` · `stockOut` · `stockAdjust` · `stockReverse` · `viewStockDocs`
**ค่าเริ่มต้น: admin = true, ที่เหลือ = false** · toun เปิดให้คนอื่นเองได้ที่แท็บ 🔐 สิทธิ์ ใน stock.html

### ⚠️ Supabase หลับได้
Free tier จะ **auto-pause เมื่อไม่มีใครใช้** (เจอจริงตอน 30 ก.ค. — เงียบไป 3 เดือนแล้วหลับ)
อาการ: ต่อไม่ติด / timeout → เช็ค `get_project` ดู `status`
ถ้าเป็น `RESTORING` รอสักครู่แล้วลองใหม่ · ถ้าหลับอยู่ แค่ยิง request ก็ปลุกได้

---

## 4. ข้อมูลจริงที่ใส่ไว้แล้ว

วัตถุดิบ **15 รายการ** จากรายงานกระทรวงอุตสาหกรรม (iSingleForm) ของ **บจก. เอส.เอ.เอส. กิจอนันต์** รอบเดือน **พ.ค. 2569**

มูลค่าคงคลังรวม **135,406.80 บาท** · หมวด 7 หมวด · ใกล้หมด 3 รายการ

**หมวดหมู่เรียงตามลำดับงานจริง** (ไม่ใช่ตามตัวอักษร):
สี → สีโป๊ว → แลคเกอร์ → ทินเนอร์/ตัวผสม → วัสดุขัด → เคมีขัดเงา → วัสดุสิ้นเปลือง

### 📊 ABC — ข้อมูลเชิงธุรกิจที่สำคัญ

ต้นทุนวัตถุดิบ **≈ 157,600 บาท/เดือน** และกระจุกตัวมาก:

| อันดับ | วัสดุ | ต้นทุน/เดือน | % |
|---|---|---|---|
| 1 | สีจริง | 32,500 | 21% |
| 2 | สีพื้น | 23,500 | 15% |
| 3 | ทินเนอร์ | 22,500 | 14% |
| 4 | แลคเกอร์ | 22,050 | 14% |
| 5 | กาวผสมสี | 17,280 | 11% |
| | **รวม 5 ตัว** | **117,830** | **75%** |

→ **5 รายการ = 75% ของต้นทุน** · อีก 10 รายการรวมกันแค่ 25% (ถุงมือยางทั้งเดือน 200 บาท)
**ข้อเสนอ:** กลุ่ม A (5 ตัว) บันทึกทุกครั้งที่เบิก · กลุ่ม B/C (10 ตัว) นับเดือนละหน

---

## 5. ฟีเจอร์ที่มีแล้วใน stock.html

3 แท็บ: **📦 คลังปัจจุบัน · 📈 รายเดือน · 🔐 สิทธิ์** (แท็บสิทธิ์เห็นเฉพาะ admin)

| ฟีเจอร์ | รายละเอียด |
|---|---|
| Movement ledger | ทุกการเปลี่ยนแปลง = 1 แถว · ยอดคำนวณจากประวัติ |
| ตาราง PC | คอลัมน์ตรงฟอร์มกระทรวง · จอเล็กเป็นการ์ด สลับอัตโนมัติที่ 1024px |
| เบิกผูก PO | `ref_type='po'`, `ref_id=<เลข PO>` · เลือกเฉพาะงานที่ยังไม่ Complete |
| ต้นทุนเฉลี่ยเคลื่อนที่ | ตีตราราคาลงทุกแถว (`unit_cost`) → แก้ราคาวันนี้ไม่กระทบเดือนก่อน |
| รายเดือนย้อนหลัง | คงคลังปลายเดือน · ซื้อ · ใช้ · ปรับยอด + กราฟ 2 ตัว |
| วิเคราะห์ราคา | แยก "จ่ายเพิ่มเพราะของแพงขึ้น" vs "เพราะใช้เยอะขึ้น" (price/volume variance) |
| เอกสารรับเข้า | เลขใบส่งของ · ผู้ขาย · รูป (จำกัดการดูด้วย `viewStockDocs`) |
| ค้นหา + เรียง | ค้นชื่อ/หมวด/หมายเหตุ · กดหัวคอลัมน์เรียง |
| เครื่องคิดเลข | ช่องจำนวนพิมพ์ `3*5` ได้ + แสดงผลลัพธ์สด |
| ค่ากลาง | `qty_target` → ป้าย "ควรสั่ง N" อัตโนมัติ |
| ยกเลิกรายการ | ออกรายการกลับ ไม่ลบประวัติ · เดิมขึ้นขีดฆ่า + ป้าย "ถูกยกเลิกแล้ว" |
| **รายวัน** 🆕 | แท็บใหม่ — ความเคลื่อนไหวทุกวัสดุรวมกัน จัดกลุ่มตามวัน · ชิป วันนี้/7 วัน/เดือนนี้/เลือกช่วง · กรองตามประเภท · ค้นชื่อวัสดุ/ผู้บันทึก/เลขใบส่งของ/PO · สรุปยอดรายวันบนหัวข้อแต่ละวัน · ดาวน์โหลด CSV |
| **ประวัติแบบแบ่งหน้า** 🆕 | หน้ารายละเอียดวัสดุโหลดทีละ 20 + ปุ่ม "ดูรายการเก่ากว่านี้" (เดิมตัดที่ 20 แล้วจบ) · แสดงวันที่+เวลาจริงแทน "N วันก่อน" |
| **นำเข้า/ส่งออก Excel** 🆕 | แม่แบบ .xlsx 2 แผ่น (ข้อมูล + วิธีใช้ที่แนบรายชื่อวัสดุจริง) · อ่านวันที่ พ.ศ. ทั้ง 2 และ 4 หลัก · **หน้าตรวจก่อนนำเข้าบอกทีละแถวว่าผิดตรงไหน ยังไม่เขียนอะไรจนกดยืนยัน** · เขียนผ่าน `apply_stock_movement` เหมือนกดปุ่มปกติ |
| **บันทึกย้อนหลัง** 🆕 | `p_created_at` — แถวที่นำเข้าลงวันที่จริงที่ของเคลื่อนไหว ไม่ใช่วันที่กด import (ดู `db/2026-08-14-stock-backdating.sql`) |
| **พิมพ์รายงาน** 🆕 | ตารางยาวต่อเนื่อง A4 · หัวตารางซ้ำทุกหน้า · คั่นหัวข้อรายวัน · สรุปยอดบนหัวกระดาษ · ช่องลงชื่อ 3 ช่อง |
| รายงานกระทรวง | ตาราง iSingleForm + คัดลอก (TSV) + ดาวน์โหลด CSV (มี BOM ภาษาไทยไม่เพี้ยน) |
| Realtime + offline | sync ข้ามเครื่อง · sw.js cache v36 |

---

## 6. Decision log — ทำไมถึงเลือกแบบนี้

**ต้นทุนเฉลี่ยเคลื่อนที่ ไม่ใช่ FIFO**
สี/ทินเนอร์เทรวมถังกัน แยกไม่ออกว่าลิตรไหนมาจากล็อตไหน FIFO layers จึงไม่ตรงกับของจริงและซับซ้อนกว่าโดยไม่ได้ประโยชน์เพิ่ม

**ตีตราราคาลงทุกรายการ (`unit_cost`)**
เดิมมูลค่าคิดจาก `qty × cost_per_unit` ซึ่งเป็นช่องเดียวที่แก้ได้ → พอซื้อของแพงขึ้นแล้วอัปเดตราคา **มูลค่าของเดือนที่ผ่านไปแล้วก็เปลี่ยนตาม** ประวัติจึงเชื่อถือไม่ได้ พอตีตราแล้ว การอ่านย้อนหลังก็แค่บวกเลข = ถูกต้องขึ้นและเร็วขึ้นพร้อมกัน

**ใช้ `ref_type`/`ref_id` เดิม แทนการเพิ่ม `po_number`**
ตอนจะเพิ่มคอลัมน์พบว่าตารางมี polymorphic pair อยู่แล้ว → ใช้ของเดิม (`ref_type='po'`) ไม่สร้างคอลัมน์ซ้ำซ้อน

**ปรับยอดในรายงานกระทรวงถูกรวมเข้า "ปริมาณที่ใช้"**
ฟอร์มไม่มีช่องปรับยอด แต่บังคับ `ยอดยกมา + รับ − ใช้ = คงเหลือ` ถ้าไม่รวมจะส่งไม่ผ่าน
ของที่นับขาด = ของหายไปจริง → รายงานเป็นการใช้ แล้วเขียนกำกับในช่องหมายเหตุ

**ห้ามใช้ `qty` ของ adjust เป็นส่วนต่าง**
`adjust` เก็บยอดใหม่แบบสัมบูรณ์ (UI ก็เขียนว่า "ยอดคงเหลือใหม่") ถ้าอ่านเป็นส่วนต่าง การนับสต๊อกจะกลายเป็นการซื้อเข้า → ต้องคำนวณจาก `balance_after` chain เสมอ

**สิทธิ์ของ admin ล็อก 3 ชั้น**
UI disabled + `savePerm` ปฏิเสธ + `loadRolePermissions` ไม่สนใจแถว admin
ถ้า admin ปิดสิทธิ์ตัวเองได้จะไม่มีทางกลับเข้าไปแก้ นอกจากงัด DB

---

## 7. คำแนะนำเชิงธุรกิจที่ให้ไว้แล้ว (toun ยังไม่ตัดสินใจ)

### บาร์โค้ด — **ยังไม่ควรซื้อ**
- SKU แค่ 15 รายการ · ถังสีไม่มีบาร์โค้ดที่ใช้ได้ · สีเบิกแบบ "ตัก" ไม่ใช่ "หยิบชิ้น" → ยิงแล้วยังต้องพิมพ์จำนวนอยู่ดี
- **ทางที่ดีกว่า:** พิมพ์ **QR ติดที่ชั้นวาง** (ไม่ใช่ที่ถัง — ถังหมดแล้วทิ้ง QR หายไปด้วย) ส่องด้วยกล้องมือถือที่มีอยู่ ต้นทุน ~50 บาท
- เกร็ด: เครื่องยิงบาร์โค้ดทำงานเหมือน**คีย์บอร์ด** ถ้าวันหน้าซื้อมา ระบบปัจจุบันรองรับได้เลยแค่เพิ่มช่องค้นหา

### แอปถ่ายรูปอ่านตัวเลข (OCR) — **ควรทำ แต่ไม่ใช่อันแรก**
- **เป้าหมายแรกควรเป็น "ใบส่งของที่พิมพ์มา"** ไม่ใช่ลายมือช่าง (พิมพ์ = อ่านแม่น, ลายมือเปื้อนสี = เสี่ยง)
- ความเสี่ยงหลักคือ **"ผิดแบบเงียบ"** — อ่าน `-13` เป็น `-18` ไม่มีอะไรดูผิดแต่สต๊อกเพี้ยน 6,500 บาท → ต้องมีหน้าจอยืนยันทุกครั้ง
- **เคล็ดลับ:** ออกแบบฟอร์มกระดาษเองแล้วพิมพ์แจก (พิมพ์ชื่อวัสดุไว้ล่วงหน้า ช่างเขียนแค่ตัวเลขในช่อง) → ความแม่นกระโดดขึ้นมาก
- ค่า AI อ่านภาพ < 1 บาท/ภาพ ≈ 30 บาท/เดือน · เวลาพัฒนา ~1-2 สัปดาห์

### เครื่องชั่งผสมสีต่อคอม — **อย่าซื้อเพื่อเอาข้อมูล**
- ถ้าเหตุผลคือ "ให้ระบบได้ข้อมูลอัตโนมัติ" → ไม่คุ้มชัดเจน
- **แต่ผู้ผลิตสีมักให้ยืม/แถม** แลกกับการซื้อสีแบรนด์เขา → **โทรถามตัวแทน Nippon ก่อน ต้นทุน 0 บาท**
- ⚠️ **คำถามที่ยังไม่ได้คำตอบ:** รายการวัตถุดิบ 15 รายการ**ไม่มีแม่สี (tinter) แยกตัว** → toun อาจ**สั่งสีผสมสำเร็จตามเบอร์** ไม่ได้ผสมเอง ถ้าใช่ เรื่องเครื่องชั่งตกไปทั้งหมด **ควรถามให้ชัดก่อนทำอะไรต่อ**

---

## 8. งานที่เหลือ

### ⏸️ พักไว้ (toun สั่งพัก)
- **ข้อ 4: นับสต๊อก (stocktake)** — ตอนนี้ปรับยอดได้ทีละรายการ แต่ยังไม่มีโหมดนับทั้งชุดเทียบยอด **งานวิจัยจัดเป็น must-have**
- **🎨 ระบบบันทึกการผสมสี** — toun สั่งพัก 2026-08-13 ("เรื่องสีขอพัก เป็น list ที่ต้องทำภายหลัง")
  **บริบทครบทั้งหมดอยู่ที่ `docs/BACKLOG-COLOR-MIXING.md`** — โรงงานผสมสีเอง มีเครื่องวัดสีแล้ว
  โปรแกรม Nippon export ได้ เครื่องชั่งไม่ต่อคอม · โครงตาราง `color_formulas` + `color_batches` เสนอไว้แล้ว

### ⚠️ ผลตรวจการใช้งานจริง (2026-08-13) — โมดูลเสร็จ แต่ยังไม่มีใครใช้
| ตัวชี้วัด | ค่า |
|---|---|
| วัสดุในคลัง | 15 รายการ · มูลค่า 135,407 บาท |
| รายการเคลื่อนไหวทั้งหมด | **15 แถว = ยอดตั้งต้นล้วน** (`initial` วันที่ 2026-07-25 ชุดเดียว) |
| รับเข้า / เบิกออก / ปรับยอด / ยกเลิก | **0 / 0 / 0 / 0** |
| รูปใบส่งของ · เบิกผูก PO | **0 · 0** |
| `qty_target` (ค่ากลาง) | **0 จาก 15** → ป้าย "ควรสั่ง N" ไม่เคยแสดงเลย (`reorderQty()` คืน 0) |
| ต่ำกว่ายอดขั้นต่ำแล้ว | **3 รายการ** — ทินเนอร์ (80/250) · กระดาษทรายขัดน้ำ (140/290) · สีโป๊วแดง (15/18) |

### 🧪 ผลทดสอบครบลูป (2026-08-13) — เจอ 6 บั๊ก แก้แล้วทั้งหมด

**วิธีทดสอบ:** (1) อ่านโค้ด plpgsql จริงด้วย `pg_get_functiondef()` (2) ยิง RPC จริงบน**วัสดุทดสอบชั่วคราว**
ครบทุกเส้นทาง — รับเข้า/เบิก/ปรับยอด/ยกเลิก/ยกเลิกซ้ำ/ยกเลิกตัวยกเลิก/จำนวนติดลบ/ประเภทผิด/เบิกเกินยอด
แล้วลบวัสดุทดสอบทิ้ง ตรวจว่ากลับมา 15 รายการ · 15 movement · 135,406.80 บาท เป๊ะ
(3) ขับ UI ด้วย Playwright ทั้งจอ PC (1440px) และมือถือ (Pixel 7) โดยถอดสูตร server ลง shim — **43/43 ผ่านทั้งสองจอ**

| # | บั๊ก | ผลกระทบจริงที่วัดได้ |
|---|---|---|
| BUG38 | `#toast` ไม่มี `pointer-events:none` → บังปุ่มถาวรหลัง toast แรก | ปุ่มยืนยันบนมือถือกดไม่ติด (กระทบ `stock.html` + `erp.html` + `po-mobile.html`) |
| BUG39 | รายการ "ยกเลิก" ถูกนับเป็นการซื้อเข้า | เดือน ส.ค. รายงานซื้อ **11,397 บาท ทั้งที่ซื้อจริง 8,375 (+27%)** · รายงานกระทรวงทินเนอร์ รับ 210/ใช้ 140 แทน 150/80 |
| BUG40 | มูลค่าคงคลังปลายเดือนคิดที่ราคาวันนี้ | มิ.ย. ขึ้น 29,981 แทน 29,300 หลังซื้อล็อตใหม่ — **ประวัติเปลี่ยนย้อนหลัง** |
| BUG41 | เบิกเกินยอดได้ → ติดลบ + ค่าเฉลี่ยราคาถูกล้าง | เบิก 9,999 จากของ 140 ผ่าน เหลือ −9,859 · รับเข้าตอนติดลบทำต้นทุนเดิมหายทั้งหมด |
| BUG42 | การ์ด "เบิกใช้รอบนี้" ใช้ราคาเฉลี่ยวันนี้ | 104,653 แทน 104,437 |
| BUG43 | `loadAllMovements` ไม่มี `.limit()` | ระเบิดเวลา: พอ ledger ถึง 1,000 แถว รายงานรายเดือนจะค้างอยู่ในอดีตถาวร ไม่มี error |

### 📱 ตรวจการใช้งานบนมือถือ (2026-08-13) — 3 ขนาดจอจริง

ทดสอบบน **iPhone SE (320px) · iPhone 14 (390px) · Pixel 7 (412px)** — **42/42 ผ่าน**
ตรวจ: ล้นขอบจอแนวนอน · สลับเป็นการ์ดแทนตาราง · ขนาดปุ่มสำหรับนิ้ว · `touch-action` ครบ ·
ป๊อปอัพอยู่ในจอ · **กดปุ่มโดนจริงด้วย `elementFromPoint`** · ตารางกว้างเลื่อนในกรอบตัวเอง ·
รายงานกระทรวงเปิดบนมือถือได้ · ฟอร์มเพิ่มวัสดุเลื่อนถึงปุ่มบันทึก

| # | บั๊กที่เจอเพิ่มจากรอบมือถือ | รายละเอียด |
|---|---|---|
| BUG44 | ปุ่ม "ยืนยัน" อยู่ใต้ขอบจอบนมือถือจอสั้น | iPhone SE + iPhone 14 เปิดป๊อปอัพรับเข้ามาไม่เห็นปุ่ม ต้องเลื่อนหา · Pixel 7 จอยาวกว่าเลยไม่เจอ → แก้ด้วย `position: sticky` ตรึงปุ่มท้ายกล่อง |
| BUG45 | ยอดตั้งต้นถูกนับเป็น "ซื้อเข้า" | **เห็นจากกราฟบนจอมือถือ** แท่งเดือนแรกสูงผิดสัดส่วน → เดือนที่เปิดใช้ระบบจะรายงานว่าซื้อ 135,407 บาท ทั้งที่ไม่ได้ซื้ออะไร |

**สิ่งที่ทดสอบแล้วถูกต้อง ไม่ต้องแก้:** ต้นทุนเฉลี่ยเคลื่อนที่ทุกกรณี (รวมรับเข้าตอนยอดศูนย์) · ตีตราราคาลงทุกแถว ·
`adjust` เป็นค่าสัมบูรณ์ · ล็อกแถวกันยอดหาย · ยกเลิกซ้ำ/ยกเลิกตัวยกเลิกถูกปฏิเสธ · เบิกผูก PO (เลือกเฉพาะงานที่ยังไม่ปิด) ·
เครื่องคิดเลขในช่องจำนวน · ใบส่งของ/ผู้ขาย/รูป · ค้นหา/เรียง · เพิ่ม-แก้-ลบวัสดุ · สิทธิ์ (แถว admin ล็อกไว้จริง) ·
`activity_log` ครบทุก action · สมการกระทรวงบาลานซ์ทุกแถว

**ยังค้าง (ตั้งใจไม่แก้รอบนี้):** ประวัติในหน้ารายละเอียดโหลดแค่ 20 แถวล่าสุด ไม่มีปุ่ม "ดูเพิ่ม" → รายการเก่ากว่านั้นกดยกเลิกไม่ได้

**สาเหตุหลักที่พบ:** `stockIn` / `stockOut` / `stockAdjust` / `stockReverse` / `viewStockDocs`
เป็น `true` เฉพาะ **admin (toun คนเดียว)** · supervisor / office / manager / staff = `false` ทั้งหมด
→ ผู้ใช้อีก 9 คนเปิดหน้าคลังได้แต่**กดบันทึกอะไรไม่ได้เลย** ระบบจึงไม่มีทางถูกใช้ในทางปฏิบัติ
แก้ได้ทันทีในแท็บ **🔐 สิทธิ์** ของ `stock.html` (admin เท่านั้นที่เห็น) — ไม่ต้องแก้โค้ด

### 🎨 ค้างอยู่ — Design ใหม่ (พร้อม push แล้ว รอ toun ตอบ)

**Session ที่ทำ:** `session_018bMyBUYL3d7grhzZZCAubA` — "Design: ERP หน้าแรกสไตล์ Modernist"
สถานะ: **IDLE / BLOCKED** · tag `omelette-handoff` · env `env_011111111111111111111114`

**สถานะที่ session นั้นรายงานเอง:**
> `repo ready for push; awaiting GitHub URL + method (A/B)`
> ต้องการ: URL ของ repo + เลือก **A (branch ใหม่)** หรือ **B (merge เข้าโครงสร้างเดิม)**

**Artifact ที่ทำไว้แล้ว 7 หน้า** — สังเกตว่า**ไม่ใช่แค่ ERP Home** แต่ redesign ทั้งระบบรวม PO app ด้วย:

| # | หน้า | URL |
|---|---|---|
| 1 | ERP Home v2 — ระบบจัดการโรงงาน | `claude.ai/code/artifact/4f69c309-e4f1-411e-b5ab-f0c48be98f09` |
| 2 | ERP โรงงานพ่นสี — ภาพรวมทุกหน้า | `.../0bb82683-7e55-4f55-a03c-021d95650203` |
| 3 | PO Tracker Dashboard | `.../c07fda43-4748-4c4e-b4a4-137d35774abd` |
| 4 | PO ทั้งหมด | `.../365d7586-0590-436a-a9d6-3b4516b68932` |
| 5 | สร้าง PO | `.../a60d4d2f-e9a1-47db-8ce8-8c1832e9d71b` |
| 6 | QC Inspection | `.../33c29153-2961-4ab3-a70a-0edbb4ba9f17` |
| 7 | Archive | `.../396bc96b-7dbf-41f6-aaab-09b5a91903eb` |

**⚠️ ข้อควรระวัง:** ดีไซน์ครอบคลุมหน้าของ PO app (Dashboard/PO ทั้งหมด/สร้าง PO/QC/Archive) ซึ่งเป็นไฟล์ที่**คนงานใช้ production อยู่ทุกวัน** → ต้อง review diff ก่อนแตะ ไม่ใช่ merge ตรงเข้า main

**คำแนะนำ: เลือก A (branch ใหม่)** เพราะ main = production คนงานใช้อยู่ · ดีไซน์กินหน้า PO ด้วย · ตรงกับวินัย branch ที่ใช้อยู่แล้ว (ERP/stock)
ชื่อ branch ที่เสนอ: `design`

**สิ่งที่ session นี้ (ERP/stock) ทำไม่ได้ — ลองครบ 3 ทางแล้ว:**
| วิธี | ผล |
|---|---|
| `DesignSync` MCP | ❌ ต้องการ `/design-login` ซึ่งใช้ได้เฉพาะเทอร์มินัลจริง |
| `WebFetch` artifact | ❌ network allowlist ของ sandbox บล็อก `*.frame.claudeusercontent.com` (ตัว artifact auth ผ่าน แต่โหลดเนื้อหาไม่ได้) |
| `SendMessage` ข้าม session | ❌ `ListAgents` = "No reachable agents" — cross-session messaging ไม่เปิดใน environment นี้ |

→ **ทางเดียวที่ได้ผล: ให้ toun ไปตอบใน session ดีไซน์เอง** แล้ว session นั้น push เข้า repo ตรง ๆ
จากนั้น session ฝั่งนี้แค่ `git fetch` ก็เห็นไฟล์

### 🔮 อนาคต (ตาม flowchart ERP)
🛒 จัดซื้อ · 👥 CRM ลูกค้า · 🏭 ซัพพลายเออร์ · 📊 รายงาน&วิเคราะห์
🔗 บัญชี/ภาษี = **เชื่อมโปรแกรมสำเร็จรูป ไม่สร้างเอง** (เสี่ยงกฎหมาย)

---

## 9. วิธีทำงานกับโปรเจคนี้ (บทเรียนจาก session นี้)

**เครื่องมือ**
- Supabase ต่อผ่าน **MCP เท่านั้น** — `curl` จาก sandbox ถูกบล็อก (HTTP 000)
- เปิดเว็บ production จาก sandbox **ไม่ได้** — ตรวจ deploy ผ่าน `mcp__github__actions_list` (ผลลัพธ์ใหญ่มาก ต้อง parse ด้วย python จากไฟล์ที่มันเซฟไว้)
- ทดสอบ UI ด้วย **Playwright + mock Supabase** (`page.route` ดัก `.supabase.co`) — ได้ผลจริงโดยไม่แตะข้อมูล production

**กฎที่ต้องระวัง**
- เขียน commit message **อย่าใช้ backtick** — shell จะตีความเป็นคำสั่ง (เจอมาแล้ว) ใช้ `git commit -F -` แทน
- แก้ไฟล์ที่ถูก cache (`stock.html`/`erp.html`/`po-*.html`) **ต้อง bump `CACHE_NAME` ใน sw.js**
- ก่อน merge เข้า main ตรวจว่าไฟล์ PO เดิมไม่ถูกแตะ:
  ```bash
  git diff --stat <prev>..main -- po-mobile.html po-desktop.html index.html manifest.json
  # ต้องว่าง
  ```
- Playwright: ปิด modal ก่อนกดปุ่มอื่น ไม่งั้น backdrop จะบัง

**สไตล์ที่ toun ชอบ**
- ตอบเป็นข้อ ๆ มีตาราง สรุปท้าย
- **อธิบายว่าทำไม ไม่ใช่แค่ทำอะไร** — และบอกข้อจำกัด/ความเสี่ยงตรง ๆ
- ให้คำแนะนำแบบ pitch ลูกค้า: เข้าใจง่าย ทำตามได้ทันที
- ถ้าทำเองได้เลย → ทำเลย ไม่ต้องรออธิบาย
